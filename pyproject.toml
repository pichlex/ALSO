[project]
name = "also"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.1",
    "mlflow>=2.14,<3",
    "optuna>=4.6.0",
    "torch>=2.9.1",
    "torchvision>=0.24.1",
]
